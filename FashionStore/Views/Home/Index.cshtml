@{
    ViewBag.Title = "Trang chủ";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    /* Hero Slider */
    .hero-slider {
        position: relative;
        height: 600px;
        overflow: hidden;
        margin-bottom: 60px;
    }

    .hero-slide {
        height: 600px;
        background-size: cover;
        background-position: center;
        position: relative;
        display: flex;
        align-items: center;
    }

    .hero-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(135deg, rgba(26, 26, 26, 0.7), rgba(45, 45, 45, 0.5));
    }

    .hero-content {
        position: relative;
        z-index: 2;
        color: white;
        max-width: 600px;
    }

    .hero-content h1 {
        font-family: 'Playfair Display', serif;
        font-size: 56px;
        font-weight: 700;
        margin-bottom: 20px;
        line-height: 1.2;
    }

    .hero-content p {
        font-size: 20px;
        margin-bottom: 30px;
        line-height: 1.6;
    }

    .btn-hero {
        background: #c9a063;
        color: white;
        padding: 15px 40px;
        border-radius: 50px;
        font-size: 16px;
        font-weight: 600;
        text-decoration: none;
        display: inline-block;
        transition: all 0.3s ease;
        box-shadow: 0 5px 20px rgba(201, 160, 99, 0.4);
        border: 2px solid #c9a063;
    }

    .btn-hero:hover {
        background: white;
        color: #c9a063;
        transform: translateY(-3px);
        box-shadow: 0 8px 30px rgba(201, 160, 99, 0.6);
    }

    /* Features Section */
    .features-section {
        padding: 60px 0;
        background: #f8f9fa;
    }

    .feature-card {
        text-align: center;
        padding: 30px 20px;
        transition: all 0.3s ease;
    }

    .feature-icon {
        width: 70px;
        height: 70px;
        background: linear-gradient(135deg, #c9a063, #d4b072);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 20px;
        font-size: 28px;
        color: white;
        transition: all 0.3s ease;
    }

    .feature-card:hover .feature-icon {
        transform: scale(1.1) rotate(5deg);
    }

    .feature-card h5 {
        font-family: 'Playfair Display', serif;
        font-size: 20px;
        color: #1a1a1a;
        margin-bottom: 10px;
        font-weight: 600;
    }

    .feature-card p {
        color: #666;
        font-size: 14px;
        margin: 0;
    }

    /* Section Title */
    .section-header {
        text-align: center;
        margin-bottom: 50px;
    }

    .section-title {
        font-family: 'Playfair Display', serif;
        font-size: 42px;
        font-weight: 700;
        color: #1a1a1a;
        margin-bottom: 15px;
    }

    .section-subtitle {
        color: #666;
        font-size: 18px;
    }

    .section-divider {
        width: 80px;
        height: 3px;
        background: linear-gradient(90deg, transparent, #c9a063, transparent);
        margin: 20px auto;
    }

    /* Products Section */
    .products-section {
        padding: 60px 0;
        background: white;
    }

    .product-card {
        background: white;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0 5px 20px rgba(0,0,0,0.08);
        transition: all 0.3s ease;
        margin-bottom: 30px;
        position: relative;
    }

    .product-card:hover {
        transform: translateY(-10px);
        box-shadow: 0 15px 40px rgba(0,0,0,0.15);
    }

    .product-image {
        position: relative;
        overflow: hidden;
        height: 300px;
        background: #f8f9fa;
    }

    .product-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.5s ease;
    }

    .product-card:hover .product-image img {
        transform: scale(1.1);
    }

    .product-badge {
        position: absolute;
        top: 15px;
        right: 15px;
        background: #e74c3c;
        color: white;
        padding: 5px 15px;
        border-radius: 20px;
        font-size: 12px;
        font-weight: 600;
        z-index: 1;
    }

    .product-badge.new {
        background: #2ecc71;
    }

    .product-info {
        padding: 20px;
    }

    .product-name {
        font-family: 'Playfair Display', serif;
        font-size: 18px;
        font-weight: 600;
        color: #1a1a1a;
        margin-bottom: 10px;
        min-height: 50px;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    .product-price {
        margin-bottom: 15px;
    }

    .price-original {
        text-decoration: line-through;
        color: #999;
        font-size: 14px;
        margin-right: 8px;
    }

    .price-discount {
        color: #e74c3c;
        font-size: 20px;
        font-weight: 700;
    }

    .price-regular {
        color: #1a1a1a;
        font-size: 20px;
        font-weight: 700;
    }

    .btn-detail {
        background: #1a1a1a;
        color: white;
        padding: 10px 25px;
        border-radius: 25px;
        text-decoration: none;
        display: inline-block;
        font-size: 14px;
        font-weight: 600;
        transition: all 0.3s ease;
        border: 2px solid #1a1a1a;
    }

    .btn-detail:hover {
        background: #c9a063;
        border-color: #c9a063;
        color: white;
        transform: translateX(5px);
    }

    /* Categories Section */
    .categories-section {
        padding: 60px 0;
        background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
    }

    .category-card {
        position: relative;
        height: 350px;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        transition: all 0.3s ease;
        margin-bottom: 30px;
    }

    .category-card:hover {
        transform: scale(1.03);
        box-shadow: 0 15px 40px rgba(0,0,0,0.2);
    }

    .category-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.5s ease;
    }

    .category-card:hover .category-image {
        transform: scale(1.1);
    }

    .category-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(to bottom, transparent, rgba(0,0,0,0.8));
        display: flex;
        align-items: flex-end;
        padding: 30px;
    }

    .category-name {
        font-family: 'Playfair Display', serif;
        font-size: 28px;
        font-weight: 700;
        color: white;
        margin: 0;
    }

    /* Newsletter Section */
    .newsletter-section {
        padding: 80px 0;
        background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
        color: white;
        text-align: center;
    }

    .newsletter-section h2 {
        font-family: 'Playfair Display', serif;
        font-size: 42px;
        margin-bottom: 15px;
    }

    .newsletter-section p {
        font-size: 18px;
        margin-bottom: 40px;
        opacity: 0.9;
    }

    .newsletter-form {
        max-width: 600px;
        margin: 0 auto;
    }

    .newsletter-form .input-group {
        box-shadow: 0 10px 40px rgba(0,0,0,0.3);
    }

    .newsletter-form input {
        padding: 15px 25px;
        border: none;
        border-radius: 50px 0 0 50px;
        font-size: 16px;
    }

    .newsletter-form button {
        padding: 15px 40px;
        background: #c9a063;
        border: none;
        border-radius: 0 50px 50px 0;
        color: white;
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .newsletter-form button:hover {
        background: #d4b072;
    }

    /* Responsive */
    @@media (max-width: 768px) {
        .hero-slider,
        .hero-slide {
            height: 400px;
        }

        .hero-content h1 {
            font-size: 36px;
        }

        .hero-content p {
            font-size: 16px;
        }

        .section-title {
            font-size: 32px;
        }

        .product-image {
            height: 250px;
        }

        .category-card {
            height: 250px;
        }
    }
</style>

<!-- Hero Slider -->
<div class="hero-slider">
    <div class="hero-slide" style="background-image: url('https://images.unsplash.com/photo-1441984904996-e0b6ba687e04?w=1600');">
        <div class="hero-overlay"></div>
        <div class="container">
            <div class="hero-content">
                <h1>Bộ Sưu Tập Mùa Đông 2026</h1>
                <p>Khám phá những thiết kế thời trang cao cấp, phong cách sang trọng và hiện đại cho mùa đông năm nay</p>
                <a href="@Url.Action("Index", "Product")" class="btn-hero">Khám Phá Ngay</a>
            </div>
        </div>
    </div>
</div>

<!-- Features Section -->
<section class="features-section">
    <div class="container">
        <div class="row">
            <div class="col-lg-3 col-md-6 mb-4 mb-lg-0">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-shipping-fast"></i>
                    </div>
                    <h5>Giao Hàng Nhanh</h5>
                    <p>Miễn phí ship đơn từ 500K</p>
                </div>
            </div>
            <div class="col-lg-3 col-md-6 mb-4 mb-lg-0">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-undo-alt"></i>
                    </div>
                    <h5>Đổi Trả 7 Ngày</h5>
                    <p>Đổi trả miễn phí trong 7 ngày</p>
                </div>
            </div>
            <div class="col-lg-3 col-md-6 mb-4 mb-lg-0">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-shield-alt"></i>
                    </div>
                    <h5>Thanh Toán An Toàn</h5>
                    <p>Bảo mật thông tin 100%</p>
                </div>
            </div>
            <div class="col-lg-3 col-md-6">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-headset"></i>
                    </div>
                    <h5>Hỗ Trợ 24/7</h5>
                    <p>Tư vấn nhiệt tình, tận tâm</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Categories Section -->
<section class="categories-section">
    <div class="container">
        <div class="section-header">
            <h2 class="section-title">Danh Mục Sản Phẩm</h2>
            <div class="section-divider"></div>
            <p class="section-subtitle">Khám phá bộ sưu tập theo từng danh mục</p>
        </div>

        <div class="row">
            <div class="col-lg-3 col-md-6">
                <a href="@Url.Action("Index", "Product", new { category = "ao-nam" })" style="text-decoration: none;">
                    <div class="category-card">
                        <img src="https://images.unsplash.com/photo-1617127365659-c47fa864d8bc?w=600" alt="Áo Nam" class="category-image">
                        <div class="category-overlay">
                            <h3 class="category-name">Áo Nam</h3>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-lg-3 col-md-6">
                <a href="@Url.Action("Index", "Product", new { category = "ao-nu" })" style="text-decoration: none;">
                    <div class="category-card">
                        <img src="https://images.unsplash.com/photo-1515886657613-9f3515b0c78f?w=600" alt="Áo Nữ" class="category-image">
                        <div class="category-overlay">
                            <h3 class="category-name">Áo Nữ</h3>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-lg-3 col-md-6">
                <a href="@Url.Action("Index", "Product", new { category = "quan-nam" })" style="text-decoration: none;">
                    <div class="category-card">
                        <img src="https://images.unsplash.com/photo-1624378439575-d8705ad7ae80?w=600" alt="Quần Nam" class="category-image">
                        <div class="category-overlay">
                            <h3 class="category-name">Quần Nam</h3>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-lg-3 col-md-6">
                <a href="@Url.Action("Index", "Product", new { category = "vay" })" style="text-decoration: none;">
                    <div class="category-card">
                        <img src="https://images.unsplash.com/photo-1595777457583-95e059d581b8?w=600" alt="Váy" class="category-image">
                        <div class="category-overlay">
                            <h3 class="category-name">Váy</h3>
                        </div>
                    </div>
                </a>
            </div>
        </div>
    </div>
</section>

<!-- Featured Products Section -->
<section class="products-section">
    <div class="container">
        <div class="section-header">
            <h2 class="section-title">Sản Phẩm Nổi Bật</h2>
            <div class="section-divider"></div>
            <p class="section-subtitle">Những sản phẩm được yêu thích nhất</p>
        </div>

        <div class="row">
            @if (ViewBag.FeaturedProducts != null)
            {
                foreach (var product in ViewBag.FeaturedProducts)
                {
                    <div class="col-lg-3 col-md-6">
                        <div class="product-card">
                            <div class="product-image">
                                @if (product.DiscountPrice != null && product.DiscountPrice > 0)
                                {
                                    <span class="product-badge">
                                        -@Math.Round((decimal)(((product.Price - product.DiscountPrice) / product.Price) * 100), 0)%
                                    </span>
                                }
                                <img src="@product.ImageUrl" alt="@product.ProductName">
                            </div>
                            <div class="product-info">
                                <h4 class="product-name">@product.ProductName</h4>
                                <div class="product-price">
                                    @if (product.DiscountPrice != null && product.DiscountPrice > 0)
                                    {
                                        <span class="price-original">@product.Price.ToString("N0") đ</span>
                                        <span class="price-discount">@product.DiscountPrice.ToString("N0") đ</span>
                                    }
                                    else
                                    {
                                        <span class="price-regular">@product.Price.ToString("N0") đ</span>
                                    }
                                </div>
                                <a href="@Url.Action("Details", "Product", new { id = product.Id })" class="btn-detail">
                                    Xem Chi Tiết <i class="fas fa-arrow-right ms-1"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                }
            }
            else
            {
                <div class="col-12 text-center">
                    <p style="color: #666; font-size: 16px;">Chưa có sản phẩm nổi bật</p>
                </div>
            }
        </div>
    </div>
</section>

<!-- Latest Products Section -->
<section class="products-section" style="background: #f8f9fa;">
    <div class="container">
        <div class="section-header">
            <h2 class="section-title">Sản Phẩm Mới Nhất</h2>
            <div class="section-divider"></div>
            <p class="section-subtitle">Cập nhật những xu hướng thời trang mới nhất</p>
        </div>

        <div class="row">
            @if (ViewBag.LatestProducts != null)
            {
                foreach (var product in ViewBag.LatestProducts)
                {
                    <div class="col-lg-3 col-md-6">
                        <div class="product-card">
                            <div class="product-image">
                                <span class="product-badge new">Mới</span>
                                @if (product.DiscountPrice != null && product.DiscountPrice > 0)
                                {
                                    <span class="product-badge" style="top: 55px;">
                                        -@Math.Round((decimal)(((product.Price - product.DiscountPrice) / product.Price) * 100), 0)%
                                    </span>
                                }
                                <img src="@product.ImageUrl" alt="@product.ProductName">
                            </div>
                            <div class="product-info">
                                <h4 class="product-name">@product.ProductName</h4>
                                <div class="product-price">
                                    @if (product.DiscountPrice != null && product.DiscountPrice > 0)
                                    {
                                        <span class="price-original">@product.Price.ToString("N0") đ</span>
                                        <span class="price-discount">@product.DiscountPrice.ToString("N0") đ</span>
                                    }
                                    else
                                    {
                                        <span class="price-regular">@product.Price.ToString("N0") đ</span>
                                    }
                                </div>
                                <a href="@Url.Action("Details", "Product", new { id = product.Id })" class="btn-detail">
                                    Xem Chi Tiết <i class="fas fa-arrow-right ms-1"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                }
            }
            else
            {
                <div class="col-12 text-center">
                    <p style="color: #666; font-size: 16px;">Chưa có sản phẩm mới</p>
                </div>
            }
        </div>

        <div class="text-center mt-4">
            <a href="@Url.Action("Index", "Product")" class="btn-hero">
                Xem Tất Cả Sản Phẩm <i class="fas fa-arrow-right ms-2"></i>
            </a>
        </div>
    </div>
</section>

<!-- Newsletter Section -->
<section class="newsletter-section">
    <div class="container">
        <h2>Đăng Ký Nhận Tin</h2>
        <p>Nhận thông tin về sản phẩm mới và ưu đãi đặc biệt từ FashionStore</p>
        <form class="newsletter-form">
            <div class="input-group">
                <input type="email" class="form-control" placeholder="Nhập email của bạn..." required>
                <button type="submit" class="btn">
                    Đăng Ký Ngay
                </button>
            </div>
        </form>
    </div>
</section>

@section scripts {
    <script>
    // Newsletter form handler
    document.querySelector('.newsletter-form').addEventListener('submit', function(e) {
        e.preventDefault();
        alert('Cảm ơn bạn đã đăng ký nhận tin từ FashionStore!');
        this.reset();
    });
    </script>
}